# ── Supabase (required for real orders/auth/admin) ──
# Create a project at https://supabase.com/dashboard, then paste:
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ── Server-side only (Edge Function secrets — NEVER put these in VITE_ vars) ──
# supabase secrets set RESEND_API_KEY=...
# RESEND_API_KEY=
# supabase secrets set ADMIN_EMAIL=...
# ADMIN_EMAIL=vegetacoder69@gmail.com

# ── Store defaults (used when store_settings row is absent) ──
# VITE_STORE_NAME="Cover King Panipat"
